// import { db } from '../pf2eTools-db.js';
import { Abilities } from './Abilities.js';

describe('Abilities', () => {
	it('should correctly type Abilities', async () => {
		// const allAbilities = await db.select().from(Abilities.model);
		// for (const ability of allAbilities) {
		// }
	});
});
