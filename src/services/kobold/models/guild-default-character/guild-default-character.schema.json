{
	"title": "GuildDefaultCharacter",
	"type": "object",
	"description": "A record of a character set as the default for a user in a guild",
	"properties": {
		"guildId": {
			"type": "string",
			"readOnly": true,
			"description": "The discord id of the guild for the default character."
		},
		"characterId": {
			"type": "integer",
			"description": "The internal id of the character."
		},
		"userId": {
			"type": "string",
			"description": "The discord id of the user who imported the character"
		}
	},
	"required": ["guildId", "characterId", "userId"]
}
