{
	"title": "BestiaryFilesLoaded",
	"type": "object",
	"description": "The bestiary files imported into the database",
	"properties": {
		"id": {
			"type": "integer",
			"readOnly": true,
			"description": "The id of the character record."
		},
		"fileName": {
			"type": "string",
			"description": "The name of the file that was imported"
		},
		"fileHash": {
			"type": "string",
			"description": "The most recent hash of the file that was imported"
		},
		"createdAt": {
			"type": "string",
			"format": "date-time",
			"description": "When the character was first imported"
		},
		"lastUpdatedAt": {
			"type": "string",
			"format": "date-time",
			"description": "When the character was last updated"
		}
	},
	"required": ["id", "fileName", "fileHash"]
}
