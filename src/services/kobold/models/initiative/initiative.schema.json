{
	"title": "initiative",
	"type": "object",
	"description": "A  character",
	"properties": {
		"id": {
			"type": "integer",
			"readOnly": true,
			"description": "The id of the initiative."
		},
		"channelId": {
			"type": "string",
			"description": "The id of the channel initiative is taking place in."
		},
		"gmUserId": {
			"type": "string",
			"description": "The discord id of the user who started the initiative"
		},
		"currentTurnGroupId": {
			"type": ["integer", "null"],
			"description": "The id of the group whose turn it is"
		},
		"currentRound": {
			"type": "number",
			"description": "The current round number"
		},
		"createdAt": {
			"type": "string",
			"format": "date-time",
			"description": "When the initiative was first started"
		},
		"lastUpdatedAt": {
			"type": "string",
			"format": "date-time",
			"description": "When the initiative was last interacted with"
		}
	},
	"required": ["id", "currentTurnGroupId", "channelId", "gmUserId", "currentRound"]
}
