version: '3'

services:
    client:
        container_name: client
        build:
            context: .
            dockerfile: ./kobold.dockerfile
        restart: always
        networks:
            - app_network

# Define a network, which allows containers to communicate
# with each other, by using their container name as a hostname
networks:
    app_network:
        external: true
