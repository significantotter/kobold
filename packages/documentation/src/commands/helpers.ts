export const commonTerms = {
	actionName: 'The name of the action being created.',
	characterName: 'The name of the focused character.',
};

export enum CommandDeferType {
	PUBLIC = 'PUBLIC',
	HIDDEN = 'HIDDEN',
	NONE = 'NONE',
}
