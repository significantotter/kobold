// @kobold/schema/db — Supabase table zod schemas (browser-safe)
// Only export the typed versions from supabase.zod.ts (which re-exports
// unchanged schemas from supabase.zod.generated.ts internally)
export * from './supabase.types.js';
export * from './supabase.zod.js';
